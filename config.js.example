// copy this file to config.js, and change the values below to real ones

// redis - redis login details (required)
// logentries: token for logentries.com account (optional)

module.exports = {

  development: {
    port: 3000,
    log: 1, // 1: error/warn, 2: info, 3: debug

    redis: {
      port: 6379,
      host: "localhost",
      password: null 
    },

    // will be overridden by values in redis
    live: {
    },

    logentries: null
  }, 

  production: {
    port: 3000,
    log: 1, // 1: error/warn, 2: info, 3: debug

    redis: {
      port: 6379,
      host: "localhost",
      password: null 
    },

    // will be overridden by values in redis
    live: {
    },

    logentries: null
  }
}